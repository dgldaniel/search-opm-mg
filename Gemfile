source 'https://rubygems.org'

gem 'httparty'
gem 'nokogiri'
gem 'parallel'
gem 'csv' # CSV is a standard library, but good to list for clarity
gem 'json' # JSON is a standard library, but good to list for clarity